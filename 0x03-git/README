It is important to always add a readme file. My first lessons on creating files and directories, using git and github. This is my first repository.
This repo contains dir such as:
bash
c
js (was deleted)
